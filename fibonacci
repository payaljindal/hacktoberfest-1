#include <iostream>
using namespace std;

int fib(int n) {
	if (n == 0) {
		return 0;
	}

	if (n == 1) {
		return 1;
	}

	int smallOutput = fib(n - 1);
	int smallOutputtwo = fib(n - 2);
	return smallOutput + smallOutputtwo;
}

int main() {
	cout << fib(3) << endl;
}
